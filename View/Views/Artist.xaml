<UserControl x:Class="View.Views.Artist"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <TextBlock Text="{Binding CurrentArtist.Name}" Margin="35,69,658,351"/>
        <TextBlock Text="{Binding CurrentArtist.Bio}" Margin="25,104,668,316"/>

        <ItemsControl ItemsSource="{Binding CurrentArtistSongs}" Margin="48,150,48,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Width="500" Height="100" MouseDown="Grid_MouseDown">
                        <Image Source="../Assets/Images/mockSongImage.jpg" Width="70" Height="70" HorizontalAlignment="Left" Margin="0,0,0,25" VerticalAlignment="Bottom" />
                        <Label Content="{Binding Title}" Margin="94,10,94,0" Width="312" Height="24" VerticalAlignment="Top" />
                        <Label Content="" Height="24" VerticalAlignment="Center" HorizontalAlignment="Left" Width="406" Margin="94,0,0,0" />
                        <Image Source="../Assets/Images/like.png" Width="30" Height="30" HorizontalAlignment="Right" Margin="0,0,50,40" VerticalAlignment="Bottom" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
